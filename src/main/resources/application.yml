server:
  port: 8080
  servlet:
    context-path: /portfolio

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/list_db
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASS:19940119Nk}
    